#include <stdio.h>
/**
 * write a program that prints exactly with proper grammar, but the outcome is a piece of art,, followed by a new line.
 */
int main(void)

{
		puts("\"Programming is like building a multilingual puzzle\");
		return (0);
}
